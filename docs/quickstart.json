{
  "_comment": [
    "The canonical quickstart snippets, owned by the library that has to make",
    "them true. `tests/test_quickstart_snippets.py` reconciles every string",
    "here against `tests/djangoproject/` — the Django project this repository",
    "actually runs in CI — so a snippet cannot drift from a working",
    "configuration without a test going red.",
    "",
    "docs.grantor.id vendors this file and checks its own copy against it,",
    "the same arrangement the API contract already has between the backend",
    "and the frontend: one owner, a synced copy, and a drift check on each",
    "side. Cross-repository freshness is a sync step, not a machine check.",
    "",
    "",
    "`urls` publishes three routes; `urls_sign_in_only` publishes two, for a",
    "project whose sign-out is its own. Both carry the namespace, which is",
    "load-bearing: the redirect URI, the transaction cookie's path and the",
    "system check all derive from reverse('grantor_django:callback').",
    "A quickstart that does not run costs the reader their trust in their",
    "first minute, which is the most expensive minute there is."
  ],
  "install": "pip install grantor-django",
  "installed_apps": "INSTALLED_APPS = [\n    ...,\n    \"grantor_django\",\n]",
  "settings": "GRANTOR_ISSUER = \"https://acme.api.grantor.id\"\nGRANTOR_CLIENT_ID = env(\"GRANTOR_CLIENT_ID\")\nGRANTOR_CLIENT_SECRET = env(\"GRANTOR_CLIENT_SECRET\")\nGRANTOR_CALLBACK_BASE_URL = \"https://app.example.com\"",
  "urls": "urlpatterns = [\n    path(\"auth/\", include(\"grantor_django.urls\")),\n]",
  "subject_field": "GRANTOR_SUBJECT_FIELD = \"profile.grantor_sub\"",
  "urls_sign_in_only": "from grantor_django.urls import sign_in_urls\n\nurlpatterns = [\n    path(\"auth/\", include(sign_in_urls)),\n]"
}
